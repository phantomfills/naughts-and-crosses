import { hoarcekat } from "@rbxts/pretty-react-hooks";
import React from "@rbxts/react";
import { ProfileCard } from "client/components/board/profile-card";

export = hoarcekat(() => {
	return <ProfileCard userId={585267099} />;
});
